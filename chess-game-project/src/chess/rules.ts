export function isValidMove(move: Move, board: Board): boolean {
    // Implement the logic to validate a chess move based on the rules of chess
    // This should check for piece movement rules, check for check/checkmate, etc.
    return true; // Placeholder return value
}

export function checkGameStatus(board: Board): string {
    // Implement the logic to check the current game status
    // This should determine if the game is ongoing, checkmate, stalemate, etc.
    return "ongoing"; // Placeholder return value
}